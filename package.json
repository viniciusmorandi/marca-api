{
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "postinstall": "npx playwright install --with-deps chromium",
    "build": "echo \"(Serverless API – sem build)\"",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "ajv": "^8.12.0",
    "cheerio": "^1.0.0-rc.12",
    "playwright-core": "^1.48.0"
  }
}
