{
  "name": "marca-api",
  "private": true,
  "type": "module",
  "engines": { "node": "20.x" },
  "scripts": {
    "postinstall": "npx playwright install --with-deps chromium",
    "build": "echo \"Serverless API (no build step)\"",
    "vercel-build": "npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "axios": "^1.7.7",
    "cheerio": "^1.0.0-rc.12",
    "playwright-core": "^1.48.0"
  }
}
